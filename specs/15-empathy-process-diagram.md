# Spec 15: Empathy Map / Process Diagram

## Goal
Add the last missing research/process artifact type from the Donor Hub comparison: a visual empathy map and a process timeline diagram (currently the case study only describes process in a text ledger).

## Scope
In:
- Empathy map (Feel / Say / Do / Think quadrant) for the Ben Sullivan persona, built in Figma with design tokens.
- A process timeline diagram (Empathize → Define → Ideate → Design System → Prototype) with rough day estimates, built in Figma.
- Add both to the case study, likely combined into one "Process" section replacing/extending the existing ledger-only approach section.

Out:
- Full Double Diamond workshop artifacts (affinity maps, raw research notes) — out of scope for a solo/self-research project.

## Approach
1. Build empathy map quadrant for Ben (reuse persona already established).
2. Build process timeline as connected step badges with day counts.
3. Screenshot both (can be one combined image or two), add to case study.

## Done when
- Empathy map + process diagram built in Figma, added to the case study with brief framing text.
